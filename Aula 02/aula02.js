/*

    "Hello, world!"

    * Para criar um projeto com Next JS precisamos utilizar o comando (sem aspas) "npx create-next-app nome do projeto";

    * Um script será executado e teremos a possibilidade de rodar o projeto;

    * Para iniciá-lo, utilizamos (sem aspas) "npm run dev"

    * A partir de agora as alterações de código já se refletem no que é visto no navegador.

    P. S.: O npx é um executador de pacotes (de npm).

*/
