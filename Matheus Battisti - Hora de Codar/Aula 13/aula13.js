/*

    Rotas Dinâmicas

    * Podemos criar rotas dinâmicas, ou seja, rotas que são gerados pelo próprio Next;

    * Para isso, vamos criar um arquivo no padrão "[id].js";

    * Onde "id" seria o dado que é alterado em cada página;

    * Desta maneira, temos páginas que são criadas automaticamente;
    
    * O que é útil para quando não temos controle de quantas rotas precisamos.

*/
